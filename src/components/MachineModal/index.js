import React from 'react';
import {connect} from 'dva';
import {Modal, Button} from 'antd-mobile';
import styles from './index.less'


export default MachineModal = (props) => {
  const {
    type,  // update, add
  } = props;
  return (

  )
}
